/**
 * Pinned Tool Versions
 *
 * Single source of truth for external tool versions used during sandbox setup.
 * Change versions here — all download URLs are derived from these constants.
 */

export const HOMEBREW_VERSION = '5.0.15';
export const NVM_VERSION = '0.40.4';
