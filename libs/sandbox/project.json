{
  "name": "sandbox",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/sandbox/src",
  "projectType": "library",
  "tags": ["versioning:synchronized"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/sandbox/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "build-cjs": {
      "executor": "@nx/esbuild:esbuild",
      "dependsOn": ["^build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "libs/sandbox/dist",
        "main": "libs/sandbox/src/index.ts",
        "tsConfig": "libs/sandbox/tsconfig.lib.json",
        "format": ["esm"],
        "declaration": true,
        "declarationRootDir": "libs/sandbox/src",
        "bundle": true,
        "thirdParty": false,
        "platform": "node",
        "assets": ["libs/sandbox/package.json"],
        "esbuildOptions": {
          "outExtension": { ".js": ".js" }
        }
      }
    },
    "bundle-es": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-cjs"],
      "options": {
        "commands": [
          "rm -rf libs/sandbox/dist/es-extension",
          "mkdir -p libs/sandbox/dist/es-extension",
          "if [ -d dist/apps/shield-macos/Release/AgenShieldES.app ]; then cp -r dist/apps/shield-macos/Release/AgenShieldES.app libs/sandbox/dist/es-extension/AgenShieldES.app; elif [ -d libs/sandbox/es-extension/AgenShieldES.app ]; then cp -r libs/sandbox/es-extension/AgenShieldES.app libs/sandbox/dist/es-extension/AgenShieldES.app; else echo 'WARNING: No AgenShieldES.app found'; fi"
        ],
        "parallel": false
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-cjs", "bundle-es"],
      "options": {
        "command": "node scripts/strip-dist-from-pkg.js libs/sandbox/dist/package.json"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "npm publish libs/sandbox/dist --access public --registry=https://registry.npmjs.org/"
      }
    }
  }
}
