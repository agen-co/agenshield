{
  "name": "cli-bin",
  "projectType": "application",
  "tags": ["scope:binary"],
  "targets": {
    "bundle": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/sea/apps/cli-bin"],
      "options": {
        "command": "npx tsx apps/cli-bin/esbuild.config.mts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["bundle"],
      "options": {
        "commands": [
          "node --experimental-sea-config apps/cli-bin/sea-config.json",
          "npx tsx -e \"const h = require('./tools/sea/shared/build-helpers.mts'); h.injectBlob({ binaryPath: 'dist/sea/apps/cli-bin/agenshield', blobPath: 'dist/sea/apps/cli-bin/sea-prep.blob' })\""
        ],
        "parallel": false
      }
    }
  }
}
