/**
 * Update page — renders the update wizard
 *
 * Shown when the SPA detects `mode: 'update'` from the health endpoint.
 */

export { UpdateWizard as UpdatePage } from '../components/update/UpdateWizard';
