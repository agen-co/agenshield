{
  "name": "shield-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/shield-ui/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/shield-ui"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "shield-ui:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "shield-ui:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "shield-ui:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "shield-ui:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "shield-ui:build:development"
        },
        "production": {
          "buildTarget": "shield-ui:build:production"
        }
      }
    }
  }
}
