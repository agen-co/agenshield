{
  "name": "daemon-bin",
  "projectType": "application",
  "tags": ["scope:binary"],
  "targets": {
    "bundle": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/sea/apps/daemon-bin"],
      "options": {
        "command": "npx tsx apps/daemon-bin/esbuild.config.mts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["bundle"],
      "options": {
        "commands": [
          "node --experimental-sea-config apps/daemon-bin/sea-config.json",
          "npx tsx -e \"const h = require('./tools/sea/shared/build-helpers.mts'); h.injectBlob({ binaryPath: 'dist/sea/apps/daemon-bin/agenshield-daemon', blobPath: 'dist/sea/apps/daemon-bin/sea-prep.blob' })\""
        ],
        "parallel": false
      }
    }
  }
}
